// Страница списка транзакций
// Пока это заглушка — наполним таблицей после того как
// напишем парсеры CSV и начнём сохранять данные в Supabase

export default function TransactionsPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold mb-1">Транзакции</h2>
      <p className="text-muted-foreground">
        Здесь появится таблица транзакций после загрузки выписки.
      </p>
    </div>
  );
}
